/**
 * Identifies which content set an attempt was taken against. Bump this
 * whenever the question bank changes meaningfully, so results recorded in
 * the "Results" and "AnswerDetails" sheets can be traced back to the
 * content that produced them.
 */
export const MOCK_TEST_VERSION = "tecs-mock-v1";
